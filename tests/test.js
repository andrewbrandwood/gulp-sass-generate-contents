'use strict';

var assert = require('assert'),
    es = require('event-stream'),
    gutil = require('gulp-util'),
    PassThrough = require('stream').PassThrough,
    sgc = require('../index');

describe('sass-generate-contents', function() {
	it('should fail in stream mode', function(done) {
		

	});
});
